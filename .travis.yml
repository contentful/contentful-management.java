language: java
install: mvn install -DskipTests=true -Dgpg.skip=true

jdk:
  - oraclejdk7
  - oraclejdk8

branches:
  except:
    - gh-pages

after_success:
  # TODO: Remove before making this repo public!!
  - mvn clean cobertura:cobertura coveralls:report -DserviceName=travis-pro -DrepoToken=5UrL97Hu2fY5wVbOfxivju2sfrfULaWwT
