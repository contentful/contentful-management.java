package com.contentful.java.cma.model;

/**
 * Enum holding the build in values of the UI extension parameter type.
 * <p>
 * Allowed types: String, one of Symbol, Enum, Number, Boolean.
 */
public enum CMAUiExtensionParameterType {
  Boolean,
  Enum,
  Number,
  Symbol
}
